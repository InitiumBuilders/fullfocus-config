# Full Focus runtime config

`tunnel-url.txt` — current public URL of August's Full Focus FastAPI backend (Cloudflare quick tunnel). Auto-updated by `fullfocus-tunnel.sh` whenever cloudflared rotates the URL.

Raw URL the APK polls:
https://raw.githubusercontent.com/InitiumBuilders/fullfocus-config/main/tunnel-url.txt
